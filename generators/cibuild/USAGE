Description:
	Creates a YAML based CI build file. Supports the following formats:
		- Azure DevOps - Pipelines

Example:
	yo nullfactory-xrm:cibuild

	This will create:
		A tailored *.yml build file.
